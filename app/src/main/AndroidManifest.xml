<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.isc.proyectokarr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrameAbout"></activity>
        <activity android:name=".frameBeatbox" />
        <activity android:name=".frameViolin" />
        <activity android:name=".frameTrompeta" />
        <activity android:name=".frameBateria" />
        <activity android:name=".frameTeclado" />
        <activity android:name=".frameBajo" />
        <activity android:name=".framebajo" />
        <activity android:name=".frameGuitarra" />
        <activity android:name=".frameMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>